{
  "name": "worldcup-sweepstake-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python -m src.worldcup_site.generate",
    "cf:pages:deploy": "npm run build && wrangler pages deploy site --project-name worldcup-sweepstake --config cloudflare/pages/wrangler.toml",
    "cf:worker:deploy": "wrangler deploy --config cloudflare/worker/wrangler.toml",
    "cf:deploy": "npm run cf:pages:deploy && npm run cf:worker:deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
