[project]
name = "worldcup-sweepstake-2026"
version = "0.1.0"
description = "Static Cloudflare Pages site for World Cup 2026 fixtures, results and sweepstake owners."
requires-python = ">=3.11"
dependencies = ["requests==2.32.4"]

[tool.ruff]
line-length = 100
